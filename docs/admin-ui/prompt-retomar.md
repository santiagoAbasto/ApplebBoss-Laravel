# Prompt para que otra sesión de Claude retome el trabajo

> Actualizado el 2026-09-16, al cerrar el **panel del vendedor** (`/vendedor`), que fue lo último. Se pega tal cual en una sesión nueva de Claude Code abierta en la carpeta del proyecto.

---

Hola. Vas a continuar un trabajo que empezó otra sesión de Claude en este proyecto (Laravel 13 + PHP 8.5 + Inertia 3 + React 19 + Vite 8 + PostgreSQL 18; Tailwind 3.4 a propósito, «Apple Boss», una tienda de Bolivia que importa desde EE. UU.). El proyecto está en `/Users/user/Projects/PROYECTO APPLE BOSS/ApplebBoss-Laravel` y el código, en `src/`.

IDIOMA: respóndeme siempre en español de Bolivia, tuteándome («tú»), sin voseo, claro y directo.

Antes de hacer nada, lee en este orden:
1. `docs/admin-ui/TRASPASO.md`: reglas de trabajo, entorno, estado y lo que sigue.
2. `docs/admin-ui/README.md`: la documentación de cada módulo; la sección «Grafo» explica cómo regenerar el grafo.
3. `docs/admin-ui/informes/verificacion-apple-2026-09-15.md`: cómo se verificaron los datos de la base de iPhone, las fuentes y las decisiones sobre la SIM, el 5G mmWave y la SOS vía satélite.
4. `docs/admin-ui/informes/ram-bateria-2026-09-15.md`: la RAM y la batería de cada iPhone, con sus fuentes externas (Apple no las publica).
5. `docs/admin-ui/informes/computadoras-2026-09-15.md`: las Mac en stock, sus fuentes y cómo se reconoce cada unidad.
6. La memoria del proyecto: corre `memanto recall --recent --tool claude-code` (agente `apple-boss`, servidor local en `127.0.0.1:8095`).

Reglas clave:
- Ejecuta sin pedirme permiso para cada paso. No hagas commit.
- No inicies sesión en el panel ni escribas credenciales: verifica con los tests, el build y tinker.
- Los precios nunca salen de Apple: vienen del inventario.
- Nada se inventa. Antes de cargar un dato, abre la página que lo respalda (WebFetch). Ante dos fuentes de Apple que no coinciden, vale la de EE. UU.
- Lo público nunca muestra costo, ganancia, IMEI, procedencia, notas internas ni datos del cliente (el número de serie sí se puede).
- **El vendedor tampoco ve el costo ni la ganancia.** Ve el precio, el descuento que él hizo y lo que cobró. La regla vive en `App\Support\SinCostos` y se aplica **en el servidor**: esos campos no viajan en el JSON de sus pantallas, ni en `/api/stock/*`, ni en sus PDF. No se oculta con CSS.
- **Cuidado al tocar `Layouts/PanelShell.jsx` o `Components/Panel/*`: son de los dos paneles a la vez** (administración y vendedor). Cada cambio hay que mirarlo en los dos. El color sale de la variable `--ab-acento`, no de un hex escrito a mano.

Estado:
- La base de modelos de referencia tiene 38 iPhone completos y verificados, del X al iPhone Duo. Incluye la RAM y la batería, cargadas con fuentes externas que coinciden. Solo quedan 4 datos «falta»: la RAM y la batería del Duo, y la batería solo eSIM del 18 Pro y del 18 Pro Max.
- La ficha pública del celular muestra cada dato con su propio ícono SVG. Un test lo exige.
- La ficha avisa qué unidades son solo eSIM y cuáles admiten 5G mmWave (las de EE. UU.).
- La comparativa pública de hasta 4 modelos de iPhone está en `/comparar/iphone`, con acceso desde el header, el menú del celular y el footer.
- Las fotos de cada modelo se suben en el admin: Tienda online → «Modelos y fotos». La guía está en `docs/admin-ui/guia-fotos-modelos.md`; sin foto, la comparativa dibuja una ilustración a escala.
- La base también tiene 16 Mac y la Lenovo IdeaPad Gaming 3 de la #10 (familia «pc», ficha por su MTM): las computadoras que había en stock el 2026-09-15, con esquema, textos e íconos propios. Las Mac tienen su comparativa en `/comparar/mac`. Falta el identificador del MacBook Neo. Si una unidad se vende, su ficha se conserva.
- La base también tiene 58 fichas de accesorios (tipo «producto_general», `accesorios.php`): cargadores, vidrios, protectores, fundas, cables y accesorios de marca. Al publicar un accesorio se reconoce por su nombre y sale con descripción, ficha con íconos y compatibilidad. Hay comparativas de cargadores (`/comparar/cargadores`: original de 20 W, certificado y original de 40 W) y de vidrios (`/comparar/vidrios`: Gorilla Glass y tradicional). Ningún vidrio del inventario dice «Gorilla» todavía. Detalle en `docs/admin-ui/informes/accesorios-2026-09-15.md`.
- Tienda online → «Categorías» quedó alineado: explica de qué inventario sale cada categoría, dónde se muestra, la portada de su página (`/catalogo?categoria=…`) y su título en Google. Accesorios ahora va en el inicio y sus tipos (cargadores, vidrios, protectores, fundas y cables) filtran el catálogo. La dirección web de una categoría ya no se cambia.
- Tienda online → «Colecciones» quedó alineado y, sobre todo, **cableado**: antes se podía armar una colección y no se veía en ninguna parte. Ahora cada colección tiene su página (`/coleccion/{slug}`), sale en el inicio con la sección «Colección de productos» de Portada, aparece como destino en «Menú», lleva su título y su descripción en Google con URL canónica y entra en el sitemap. En la tienda solo se muestran los productos que siguen a la venta. Ojo: la sección «Disponibles ahora» del inicio quedó encendida sin colección elegida, así que hoy ese espacio no muestra nada hasta que el usuario cree una colección y la elija en Portada.
- Tienda online → «Portada» quedó alineado y cableado: el panel dice qué muestra hoy cada sección del inicio y por qué una queda fuera, con la misma cuenta que hace la tienda. Se cableó «Ofertas» (publicaciones con precio promocional vigente), «Nuevos ingresos» se resuelve en el backend, se quitaron dos tipos de sección que la tienda no sabía dibujar, se pueden programar fechas por sección y el precio rebajado ya se ve en las tarjetas y se cobra en el carrito.
- Tienda online → «Menú» quedó alineado y cableado: los tres menús (computadora, celular y pie) con vista previa real de cada uno, «Copiar los que faltan» entre la computadora y el celular, y los arreglos de la tienda (los enlaces a otros sitios se rompían, «abrir en otra pestaña» solo servía en el pie, el verde de MYSKIN no llegaba al pie).
- Tienda online → «Páginas» quedó alineado y cableado: ahora se crean, se ordenan, se encienden y se borran desde el listado (antes solo se podía editar lo que dejó el seeder). El panel avisa cuál está encendida y sin texto, dice dónde figura cada una y al borrarla oculta los enlaces del menú que llevaban a ella.
- Tienda online → «Preguntas frecuentes» quedó alineado y cableado: cada pregunta se muestra en un solo lugar (inicio, ficha de los productos, /iphone o /seminuevos). Antes el panel ofrecía alcances que no leía nadie mientras la ficha y los hubs mostraban preguntas escritas en el código; ahora todo eso vive en la base y se edita desde el panel.
- Tienda online → «Servicios» quedó alineado y cableado: la tienda ya no muestra servicios escritos en el código cuando no hay ninguno encendido, cada tarjeta puede solo informar, abrir WhatsApp o llevar a una página, el título de la sección se cambia en Portada y la pantalla trae una guía para escribirlas.
- Tienda online → «Ubicaciones» quedó alineado y cableado: es el único lugar de la dirección, el horario (día por día, con «Abierto ahora»), el contacto y el mapa de cada local. Configuración ya no repite esos datos, el inicio muestra todos los locales encendidos y el primero es el principal (ciudad de la tienda, dirección del pie y datos para Google). El horario que se veía antes era un valor de ejemplo de la instalación y se quitó: falta cargar el horario real y la dirección con calle.
- Tienda online → «Novedades» quedó alineado y cableado: se crean como borrador, se publican o se programan con fecha y hora, la dirección queda fija al publicarlas y la foto se guarda con sus variantes. El inicio suma la sección «Novedades» de Portada; mientras no haya ninguna publicada, esa sección, el enlace del pie y el sitemap no llevan a una página vacía. `/novedades` y cada novedad pasaron a la estética del resto de la tienda. Falta que el usuario escriba la primera.
- Tienda online → «Configuración» quedó alineado y cableado, y con eso **Tienda online quedó completo**: es el WhatsApp de la tienda más la identidad que acompaña a todas las páginas (nombre, frase del pie y barra de anuncio). El nombre estaba escrito a mano en unos 30 lugares de la tienda y ahora sale del panel; la barra de anuncio tenía el texto dentro del código y ahora manda el panel (vacía, no se dibuja); se sacaron el hero y el SEO del inicio, que se editan en Portada y en «Google y redes sociales»; y el guardado por fin valida (el número de WhatsApp solo con dígitos y código de país, nombre obligatorio, largos máximos).
- Tienda online → «Trade-In» quedó alineado y cableado, con el formulario público de `/trade-in` rehecho a pedido del usuario: un cuestionario profesional de seis pasos por tipo de equipo (cuenta, bypass, IMEI, cada pieza con «Funciona / Falla / No lo probé», estado físico, batería, historial de piezas, reparaciones, accesorios y hasta 6 fotos privadas), con los caminos de menú de la ayuda de Apple para Latinoamérica, para que la cotización se acerque al valor que se confirma en la tienda. Recibe de todo: equipos Apple y también Celular Android, Laptop, PC de escritorio, Consola y Otro, con sus propias preguntas. El hero tiene el contenido en 1224 px, siluetas animadas en los márgenes de afuera y un equipo en revisión a la derecha del texto (el usuario prefiere así las animaciones: en los márgenes o en el espacio libre, nunca encima del texto). La confirmación ya no expone el nombre y el teléfono del cliente. En el panel, cada solicitud trae grado sugerido y alertas, WhatsApp con el mensaje listo, etapas con historial, aviso en el Resumen y un número junto a «Trade-In» en el menú.
- Marketing y Google → «Campañas», «Suscriptores» y «Ajustes del newsletter» quedaron alineados y cableados. El panel ahora dice si el servidor puede mandar correos (`App\Support\NewsletterEstado`: el correo del `.env` y la cola), hay un botón para mandarse un correo de prueba y la regla de «no se puede enviar» es una sola (`NewsletterCampaign::porQueNoSePuedeEnviar()`). **En producción solo falta cargar la clave del correo (`MAIL_PASSWORD`) y comprobarla con «Enviar prueba».**
- El botón de WhatsApp que flota en la tienda se rehízo: marca sólida, entrada con rebote, ondas, guiño, burbuja de saludo una vez por visita y tope en la línea del copyright. Ojo: `resources/css/app.css` **no entra en el build** (el que se compila es `resources/css/app-vite.css`); ahí estaban sus animaciones viejas y por eso nunca se movieron.

- Marketing y Google → «Google y redes sociales» quedó alineado y cableado: dice qué página está corta, larga o sin texto con las medidas de Google, avisa si `APP_URL` sigue siendo la de prueba y muestra las dos vistas previas (el resultado de Google y la tarjeta de WhatsApp). Se sumó la página de colección, que era la única que no se podía editar.
- Exportar datos → «Exportaciones» y «Exportador» quedaron alineados y cableados: el pie de los PDF sale de Configuración y de Ubicaciones (antes estaba escrito en la plantilla), cada tarjeta dice cuántos productos hay y el exportador cuenta en vivo antes de generar el PDF.
- **El panel del vendedor (`/vendedor`) quedó alineado y cableado.** Los dos paneles comparten ahora el armazón (`Layouts/PanelShell.jsx`: menú, barra de arriba, migas, cajón de celular y confirmación de salida) y las pantallas de reservas, clientes, servicios, cotizaciones y «Nueva venta», que viven en `Components/Panel/` y reciben un `prefijo` (`admin` o `vendedor`) más el `Layout`. **Si tocas una de esas piezas, estás tocando los dos paneles.** «Mi día» se rehízo con la meta del mes en morado (`#5C5E99`, el color del panel del vendedor) con inclinación 3D, aurora, arco de progreso en lima y confeti al llegar al 100 %; la meta salía escrita a mano en el controlador (10.000 Bs para todos) y ahora es `users.meta_mensual`, que carga el administrador en Usuarios y roles. «Productos en stock» pasó a búsqueda y paginado en el servidor, una lista por vez. **El vendedor no ve el costo ni la ganancia en ninguna pantalla**: la regla está en `App\Support\SinCostos` y se aplica en el servidor (sus listas, `/api/stock/*` y sus PDF), no con CSS. En Mi día, «Ganancia de hoy» se reemplazó por «Descuentos que hiciste». El costo real de cada venta lo recalcula el servidor desde el producto, así que el navegador no lo necesita. Cada panel lleva su color en una variable CSS (`--ab-acento`): #585E9F en administración y #5C5E99 en el del vendedor, con el menú en degradado de morado fuerte arriba a suave abajo. En el camino se arreglaron: la permuta que se restaba una vez por producto, `whereMonth` sin año, los egresos de toda la tienda metidos en el panel del vendedor, cuatro paginadores compartiendo `page`, `ILIKE` (solo PostgreSQL) en clientes y ventas, una ruta que apuntaba a un método inexistente, `exportarResumen` sin fechas por defecto y sin botón, los nombres de ruta distintos de cotizaciones, dos métodos que decían «Promoción enviada» sin enviar nada y la fecha de reservas corrida un día por UTC.

- **Módulo nuevo: Sistema → «Usuarios y roles»** (`/admin/usuarios`). Los roles son datos (tabla `roles`) con permisos por módulo (`App\Support\Permisos`, 14 módulos), que hace cumplir `PermisoMiddleware` en el servidor y respeta el menú. Ojo: `users.rol` era un `enum('admin','vendedor')` y por eso no entraba ningún rol nuevo; ahora es texto. El administrador siempre puede todo, el vendedor sigue con su panel `/vendedor` y hay barandas probadas (nadie se cambia el rol a sí mismo, siempre queda un administrador, un rol con gente no se borra).

- **Servicio técnico: el vendedor registra solo lo que cobra y el administrador carga el costo** (2026-09-16, pedido del usuario). Al registrar sale la nota en el momento; al administrador le llega el aviso «Falta el costo» en el Resumen y la alerta fija en Servicio técnico, con «Cargar costo» en cada fila (la nota del cliente no cambia y queda quién y cuándo cargó el costo). Mientras falta, ese servicio suma a lo cobrado pero no suma utilidad en ningún reporte. De paso se cerraron dos fugas al vendedor: la búsqueda rápida de servicios le devolvía el costo y la tabla le dibujaba una columna de ganancia.

- **Revisión de cierre en curso (2026-09-16), a pedido del usuario:** se apagó «Disponibles ahora» (sin colección); se quitaron las rutas `admin.ventas.show` y `admin.ventas.destroy`, que apuntaban a métodos inexistentes; se sacó `resources/js/ziggy.js` (una lista de rutas vieja que no se usaba y entraba en el JavaScript público); `PanelSinErroresTest` abre todas las pantallas del panel de administración; y se cableó la **portada grande** (volanta, título, texto, segundo botón y colores), que además dejó de dibujarse encima del encabezado fijo al bajar.
- **Decisiones del usuario para cerrar:** producción en **Hostinger**; la base de productos Apple y el recomendador con IA **entran antes de cerrar**; al final, **commits ordenados**.

- **Base de productos Apple: hecha** (2026-09-16). 29 fichas de tipo `producto_apple` (iPad —Wi‑Fi, salvo un iPad Pro M5 Wi‑Fi + Cellular—, Apple Watch Series 10, AirPods, Apple Pencil, Magic Mouse y 4 productos de otras marcas cargados ahí, que no entran en la comparativa), todas las del inventario, cada una con su página técnica de Apple y un ícono SVG por dato; se reconocen por el nombre del inventario, llenan la publicación y alimentan la comparativa nueva **`/comparar/apple`** (hasta 4, en el menú «Comparar»). Detalle en `docs/admin-ui/informes/productos-apple-2026-09-16.md`. El inventario de «Productos Apple» quedó corregido (nombres profesionales) y sus 49 productos reconocen su ficha; estas fichas no tienen datos pendientes. El menú lateral de los dos paneles usa la silueta del logo (`public/images/logo-appleboss-marca.png`, sin el texto de la imagen) a 36 px a la izquierda, con «Apple Boss» centrado en el menú.
- **Stack actualizado** (2026-09-16): PHP 8.5, Laravel 13, Inertia 3, React 19, Vite 8, PHPUnit 12, PostgreSQL 18 y n8n 2.39. Tailwind se queda en la 3.4 a propósito (la 4 rompe iPhone con iOS anterior a 16.4). Detalle, ajustes y cómo volver atrás en `docs/admin-ui/informes/actualizacion-stack-2026-09-16.md`.
- Pasan **563 tests** (`docker exec appleboss-app php artisan test`) y **nada está commiteado**.

Lo que sigue para cerrar el sistema, en este orden (el usuario quiere que le vayas preguntando lo que haga falta decidir):

1. **Recomendador con IA** (presupuesto + uso): los precios salen del inventario y la IA no inventa características. Antes de construirlo, pregúntale al usuario qué IA usar (proveedor y clave, que va solo en el `.env`) y dónde se muestra.
2. **Lista de producción para Hostinger:** dominio con https y `APP_URL`, `MAIL_PASSWORD` comprobado con «Enviar prueba», la cola del newsletter (en Hostinger compartido no hay Docker: tarea programada con `queue:work --stop-when-empty`), `storage:link`, cachés de Laravel y cómo subir el build. Pregúntale si es hosting compartido o VPS.
3. **Commits ordenados** con todo lo hecho, cuando el usuario dé el visto bueno.

Si aparece un producto Apple nuevo en el inventario, se agrega con «Cómo agregar un producto Apple» de `TRASPASO.md`.

Lo que necesita que yo haga algo primero. **No arranques con esto: recuérdamelo.**
- Subir las primeras fotos en Tienda online → «Modelos y fotos» (guía en `guia-fotos-modelos.md`). Recién ahí revisamos juntos la comparativa y esa pantalla.
- Revisar conmigo la unidad #26 (MacBook Pro «M5 Pro» de 512 GB) y el identificador del MacBook Neo.
- Escribir la primera novedad y cargar el horario real y la dirección con calle en Ubicaciones.
- **Producción:** cargar `MAIL_PASSWORD` y comprobarlo con «Enviar prueba» en Ajustes del newsletter, y cambiar `APP_URL` por el dominio real con https (de ahí salen las URL canónicas, el sitemap y las imágenes que se comparten).

Cómo se alinea y se cablea un módulo (así quedaron Servicios, Ubicaciones, Novedades, Trade-In, Configuración y el panel del vendedor; cópialos como modelo):
1. **Diagnóstico antes de tocar nada:** lee el controlador, el modelo, las pantallas y **dónde lo usa la tienda pública**. Busca contenido escrito en el código que reemplaza al del panel (`*_FALLBACK`, textos fijos), campos que el panel pide y nadie muestra, datos repetidos en otro módulo, valores de ejemplo de la instalación mostrados como reales (en `configuracion_tienda`, `created_at` igual a `updated_at`) y lo que Portada informa mal.
2. **Backend:** una sola consulta pública en el modelo (como `StoreService::paraLaTienda()` o `StoreLocation::paraLaTienda()`), validación con mensajes en español, textos limpios, `update` parcial para el interruptor, `reorder` con `back()`, redirecciones con mensaje (nada de JSON ni `axios`), migración si hace falta y seeder que no pise lo cargado.
3. **Pantalla** con la línea de `Components/Admin/ui.jsx` e `inventario.jsx`: PageHeader, cuatro `Stat`, `AdminGuide` con id nuevo, `Aviso`s, lista con arrastre, flechas e interruptor, `ModalEliminar` con `advertencia`, formulario con `StepCard` y columna al costado con **vista previa usando el mismo componente que dibuja la tienda** (en `Components/Store/`).
4. **Guía profesional:** pasos concretos, consejos con un ejemplo bueno y uno a evitar, y los nombres exactos de los botones de herramientas externas, confirmados en su ayuda oficial.
5. **Tienda pública:** que muestre solo lo del panel, que la sección no se dibuje sin contenido y que Portada diga por qué.
6. **Verificación sin iniciar sesión:** tests en `src/tests/Feature/<Modulo>AdminTest.php`, `npm run build`, toda la suite, vista previa interactiva (esbuild + `createRoot` con los props reales sacados con tinker y stubs de Inertia en `src/preview-tmp/`, servida en `src/public/vista-*.html`) a 1440 y 390 px, y la tienda real con datos temporales que después se dejan como estaban. En el plugin de alias de esbuild, completa la extensión (`.jsx`, `.js`): esbuild no la agrega a las rutas que devuelve un plugin; y acuérdate de pasarlo en `plugins: [alias]`, porque si no se cuela el Inertia de verdad y la pantalla muere con «usePage must be used within the Inertia component». Si el navegador muestra el bundle viejo, cámbiale la versión al `<script src="…?v=2">`. El CSS de la vista previa se toma del manifiesto **dentro de `appleboss-node`** (`/var/www/html/public/build/manifest.json`): el `src/public/build` del host está viejo. Para un formulario público, prueba también un envío real desde el navegador (los tests de PHP no pasan por el FormData de Inertia) y borra lo creado. El panel del navegador achica los anchos grandes: para ver el detalle a 1440, 1728 o 1920 px, saca capturas con Chrome sin interfaz manejado por DevTools (un script de Node con `--remote-debugging-port`, `Emulation.setDeviceMetricsOverride` y `Page.captureScreenshot` con `clip`) y guárdalas en el scratchpad. Borra la vista previa al terminar.

   **Tres trampas con las que ya nos tropezamos:**
   - **El panel del navegador de Claude congela las animaciones**, así que una pantalla cuya entrada arranca en `opacity: 0` sale **en blanco** en la captura. No es un error del código: saca esas capturas con Chrome sin interfaz. Pero sí es un aviso: una entrada animada nunca debe poder dejar contenido invisible ni un número a medio camino. El patrón del proyecto es `useEntrada()` (en `Components/Vendedor/dia.jsx`), que decide al montar si se anima; cuando no, pasa `initial={false}` y todo se dibuja ya visible y en su valor. Los contadores llevan además un `setTimeout` de respaldo. Misma lección que el botón de WhatsApp que se quedaba en `scale(0.3)`.
   - **Al mover una pantalla a `Components/Panel/`, ojo con los ayudantes declarados fuera del componente.** Si un `function Acciones(...)` de nivel de módulo usa `route(\`${prefijo}.…\`)`, `prefijo` no está en su alcance y la pantalla **se cae en blanco en los dos paneles**. Los tests no lo agarran (solo comprueban el servidor): se ve abriendo la pantalla en la vista previa. Hay que pasarle `prefijo` como prop.
   - **Abre de verdad todas las pantallas** en la vista previa, no solo una: los tests confirman que el servidor devuelve el componente correcto, no que React lo dibuje.
7. **Documentación:** sección «<Módulo> (admin)» y tabla de estado en `docs/admin-ui/README.md`, `TRASPASO.md`, este prompt (que no se contradiga con el estado: si cierras un módulo, borra la frase que decía que estaba pendiente), diagrama archify en `docs/admin-ui/diagramas/` (workflow v2, tres carriles; verifícalo con `deliver` y `visual-check`), grafo (`docs/admin-ui/grafo/mapa-fuentes.json` + `python3 docs/admin-ui/grafo/regenerar.py`) y memoria (memanto + `memanto memory sync --project-dir .`).
8. **Informe final breve:** qué estaba mal, cómo quedó, cómo se verificó y qué tiene que cargar el usuario.

Cuando termines de leer, confírmame en pocas líneas que entendiste el estado y arranca con el punto 1 de «Lo que sigue», preguntándome lo que haga falta decidir.
